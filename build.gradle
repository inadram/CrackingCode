task wrapper(type: Wrapper) {
    gradleVersion = '3.3'
}
buildscript {
    repositories {
        mavenCentral()
    }
}

apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies(){
    compile group: 'junit', name: 'junit', version: '4.12'
}